import { Conversation } from "../utils/conversation";

export async function callLLM(conversation: Conversation): Promise<void> {
  // Placeholder: will stream tokens from LLM, apply sentence-boundary
  // chunking, send each chunk to TTS, and emit audio back to frontend

  
}
